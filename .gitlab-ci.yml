# This file is a template, and might need editing before it works on your pro
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Gradle.gitlab-ci.yml

# This is the Gradle build system for JVM applications
# https://gradle.org/
# https://github.com/gradle/gradle

stages:
  - build
  - test
  - deploy
  - release
  
# image: gradle:alpine

# before_script:
#   - GRADLE_USER_HOME="$(pwd)/.gradle"
#   - export GRADLE_USER_HOME

# build:
#   stage: build
#   script: gradle --build-cache assemble
#   cache:
#     key: "$CI_COMMIT_REF_NAME"
#     policy: push
#     paths:
#       - build
#       - .gradle

# test:
#   stage: test
#   script: gradle check
#   cache:
#     key: "$CI_COMMIT_REF_NAME"
#     policy: pull
#     paths:
#       - build
#       - .gradle

# deploy:
#   stage: deploy
#   script: echo "Define your deployment script!"
#   environment: production

build-job:
  stage: build
  script:
    - echo "Hello, $GITLAB_USER_LOGIN!"

test-job1:
  stage: test
  script:
    - echo "This job tests something"



deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
  environment: production
release_job:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest              # Run this job when a tag is created
  script:
    - echo "running release_job"
    - tag_name=$(date +'%Y-%m-%d-%H-%M-%S')
    - release-cli create --description "Release created automatically for $CI_COMMIT_REF_NAME" --tag-name "$tag_name"
  # release:                               # See https://docs.gitlab.com/ee/ci/yaml/#release for available properties
  #   tag_name: "$tag_name"
  #   description: '$CI_COMMIT_TAG'

# release:
#     stage: release
#     script:
#         - echo "Making new release"
#         - curl --request POST --form "name=Release $CI_COMMIT_REF_NAME" --form "tag_name=$CI_COMMIT_TAG" --form "description=Automatic relese for $CI_COMMIT_TAG" --form "ref=$CI_COMMIT_REF_NAME" "https://gitlab-stud.elka.pw.edu.pl/api/v4/projects/$CI_PROJECT_ID/releases"
#     only:
#         - tags
